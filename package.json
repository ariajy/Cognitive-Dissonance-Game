{
  "name": "cognitive-dissonance-game",
  "private": true,
  "description": "Static game + Netlify function for registration webhook",
  "scripts": {
    "deploy": "netlify deploy --prod",
    "deploy:draft": "netlify deploy"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
