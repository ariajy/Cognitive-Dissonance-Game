# Tasks (index)

The canonical task brief for agents is **`TASK.md`** (singular).

Use **`PROJECT_STATUS.md`** for repository layout, scenario schema, and implementation snapshot.

If you are the Mobile Game Developer Agent, start with:

1. `PROJECT_STATUS.md`
2. `TASK.md`
3. `content/scenarios.js` + `game/gameLoop.js` + `core/*.js` + `ui/gameUI.js` + `index.html`

---

*This file exists so references to `TASKS.md` resolve without duplicating content.*
