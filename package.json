{
  "name": "learn-from-dissonance",
  "private": true,
  "description": "Static game + Netlify registration API (MongoDB)",
  "scripts": {
    "build": "echo Static site - no bundler",
    "deploy": "npx netlify-cli deploy --prod",
    "deploy:draft": "npx netlify-cli deploy"
  },
  "dependencies": {
    "mongodb": "^6.12.0"
  }
}
